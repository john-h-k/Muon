using System;
using Ultz.Muon.Extensions;

namespace Muon.Runtime
{
    public static class Std
    {
        /* TODO This is where we'll need to implement functions such as malloc and map them to OS-specific APIs
           (for instance malloc will map to kernel32.lib's HeapAlloc and libc.a's malloc */
    }
}